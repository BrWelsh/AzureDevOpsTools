<mah:MetroWindow x:Class="AzureDevOpsTools.Presentation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
        Height="650" Width="750" MinWidth="600" MinHeight="600"
        xmlns:local="clr-namespace:AzureDevOpsTools.Presentation.Views"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Title="{Binding Title}">
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <DockPanel LastChildFill="True">
            <StatusBar DockPanel.Dock="Bottom">
                <StatusBarItem Content="StatusBarItem" Margin="3,0,0,0" />
            </StatusBar>
            <DockPanel LastChildFill="True">
                <ContentPresenter DockPanel.Dock="Left" />
                <!-- Control Center -->
                <Button DockPanel.Dock="Top" Width="30" Height="30" Background="{StaticResource Settings_Inverse_16x}" Command="{Binding AboutDialogCommand}" />
                <ContentPresenter />
            </DockPanel>
        </DockPanel>
    </Grid>
</mah:MetroWindow>
